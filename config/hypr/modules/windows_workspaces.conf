##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# windowrule = opacity 1.0 override 1.0 override 1.0 override, class:firefox  # Set opacity to 1.0 active, 1.0 inactive and 1.0 fullscreen for firefox

# Ignore maximize requests from apps. You'll probably like this.
# windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
# windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

workspace = DP-1, 1, default:true

workspace = 1, monitor:DP-1, default:true
workspace = 2, monitor:DP-1, 
workspace = 3, monitor:DP-1, 
workspace = 4, monitor:DP-1, 

workspace = 5, monitor:DP-2, default:true
workspace = 6, monitor:DP-2, 
workspace = 7, monitor:DP-2, 
workspace = 8, monitor:DP-2, 

workspace = 9, monitor:HDMI-A-1, default:true